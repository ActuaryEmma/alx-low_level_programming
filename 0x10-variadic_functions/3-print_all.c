#include "variadic_functions.h"
#include <stdio.h>
#include <stdarg.h>
#include <stdlib.h>
/**
  * print_all - print all formats followed by a separator
  * @format: list of types of argument
  */
void print_all(const char * const format, ...)
{
	unsigned int i;
	char *var_str;
	va_list mylist;

	va_start(mylist, formart);

	printf("\n");
	va_end(mylist);
}

