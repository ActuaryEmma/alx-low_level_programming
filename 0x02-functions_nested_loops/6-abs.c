#include "main.h"

/**
  * _abs - return abolute value of an integer
  * @int: input value
  * Return: return absolute value
  */
int _abs(int num)
{
	if (num < 0)
	{
		return (-num);
	}
	else
	{
		return (num);
	}
}
